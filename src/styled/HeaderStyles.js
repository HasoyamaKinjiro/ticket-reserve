import { sizes } from '../GlobalStyles';

const appBarHeader = {
    padding: '0 24px',
    mb: sizes.globalMarginBottom
};

const toolbarHeader = {
    margin: '0 31.5px'
}

const ticketIconHeader = {
    ml: 1
}

export {
    appBarHeader,
    toolbarHeader,
    ticketIconHeader
};
