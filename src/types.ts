export type Dates = string[];

export interface Movie {
    title: string;
    genre: string;
    duration: number;
    releaseDate: string;
    rating: number;
    description: string;
    posterUrl: string;
}
